# Required
ATERA_API_KEY=

# Optional dashboard tuning
DASH_REFRESH_INTERVAL_MS=60000
DASH_STALE_AFTER_MS=300000
DASH_PENDING_KEYWORDS=pending,uptime,waiting on user,waiting on customer,in-progress,closure pending,internal escalation
DASH_CLOSED_KEYWORDS=closed,resolved,merged,deleted,spam,cancelled

# Optional test-only fixture path
# When set, getDashboardMetrics loads data from this JSON file instead of calling the API.
DASHBOARD_FIXTURE=
